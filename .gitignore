# OS / editor junk
.DS_Store
**/.DS_Store
Thumbs.db

# Large source media — NOT part of the website (kept locally only)
# 55GB of raw videos/zips; GitHub rejects files >100MB.
סרטונים/

# Local-only tooling config
.claude/settings.local.json

# Logs / temp
*.log
.cache/

# Node / Firebase
node_modules/
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log

# Secrets — NEVER commit a service account key
serviceAccountKey.json
**/serviceAccountKey.json
*serviceAccount*.json
.env
.env.*
